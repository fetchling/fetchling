# fetchling

MCP router with lazy tool loading. One endpoint in front of N MCP servers:
the model gets a small set of meta-tools (search → describe → call) instead of
every upstream catalog on every turn.

**Work in progress — not usable yet.** This version only prints a notice.

Follow along: https://github.com/fetchling/fetchling
