# fetchling

MCP router with lazy tool loading. One endpoint in front of N MCP servers:
search → describe → call, so the model only ever sees the tools it needs.

Work in progress — not usable yet. See [res/fetchling-project-plan.md](res/fetchling-project-plan.md).
